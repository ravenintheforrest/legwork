# The only required credential. Fine-grained PAT, public-repo read scope only.
GITHUB_TOKEN=

# Required for `legwork brief` and `legwork doctor` (LLM calls). Everything else runs without it.
ANTHROPIC_API_KEY=

# Optional adapters — the fleet runs fully on fixtures without any of these.
HUBSPOT_TOKEN=
SLACK_WEBHOOK_URL=
